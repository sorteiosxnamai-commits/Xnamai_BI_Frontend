# Xnamai BI Frontend

Execute `npm install` e `npm run dev`. Para produção use Docker/Render ou `npm run build`. Configure `VITE_BI_API_URL` com a URL do backend. Os dados demonstrativos permitem validar o painel antes da primeira sincronização; os endpoints reais estão documentados no backend.
"# Xnamai_BI_Frontend" 
